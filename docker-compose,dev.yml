services:
  notiffio:
    container_name: notiffio
    hostname: notiffio
    image: ghcr.io/deadcxap/bee-notiffio:dev
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
