services:
  notiffio:
    build:
      context: .
    image: ghcr.io/${GITHUB_REPOSITORY_OWNER:-local}/bee-notiffio:dev
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
